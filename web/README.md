# Niyam — web client (`web/`)

React + TypeScript + Vite SPA for **Niyam ATS**. Dev server proxies `/api` to the FastAPI backend.

```bash
npm install
npm run dev    # http://localhost:5173
npm run build  # outputs to ../static
```
